export enum MessageType {
    JOIN = "JOIN",
    ROOM_JOIN = "ROOM",
    MESSAGE = "MESSAGE",
    WHISPER = "WHISPER",
    LEAVE = "LEAVE",
    SYSTEM = "SYSTEM",
    ERROR = "ERROR",
    COMMAND = "COMMAND"
}