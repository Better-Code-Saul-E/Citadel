export enum MessageType {
    JOIN = "JOIN",
    ROOM_JOIN = "ROOM",
    MESSAGE = "MESSAGE",
    WHISPER = "WHISPER",
    ROOM_LEAVE = "LEAVE",
    SYSTEM = "SYSTEM",
    ERROR = "ERROR",
    COMMAND = "COMMAND"
}